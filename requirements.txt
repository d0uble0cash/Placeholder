# Runtime
openpyxl>=3.1.0
numpy>=1.20.0
customtkinter>=5.2.0
biopython>=1.80
Pillow>=9.0.0
# Build (only needed for packaging)
setuptools>=68.0
pyinstaller>=6.0

# ViennaRNA must be installed via system package manager:
#   macOS:  brew install viennarna
#   Ubuntu: sudo apt-get install viennarna
#   Fedora: sudo dnf install viennarna
